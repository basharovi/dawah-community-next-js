rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read, authenticated writes on all collections
    match /{collection}/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
} 